import React, { Component } from 'react';
class Event extends Component {
  render() {
    return (
        <section>
            <button type="text">
                点击
            </button>
        </section>
    );
  }
}

export default Event;
